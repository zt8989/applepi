# 03: 服务端生命周期精化 —— 心跳租约

**What to build:** 服务端生命周期收敛：已接入客户端心跳计数，无客户端满 5 分钟自动退出；SIGINT 立即退；客户端断开时进行中的流式段自动中止（已有 abort 语义，补上服务端侧清理断言）。租约实现与票 01 的基础 spawn/日志（detached、`~/.applepi/server.log`、EADDRINUSE 自愈）接合；客户端侧心跳发送归入共享 attach 函数（web 壳与 TUI 共用）。

**Blocked by:** 01（服务端与 attach 函数存在）；建议在 02 之后收尾（TUI 成为第二个心跳源后一并验证）。

**Status:** ready-for-agent

- [ ] 服务端记录客户端心跳（attach 注册 + 周期续命 API），无客户端 5 分钟自动退出；SIGINT 立即退
- [ ] 客户端（web 壳 + TUI）统一心跳实现（共享 attach 函数），单客户端退出/覆盖场景正确
- [ ] 客户端断开 → 当前流式段中止（测试：请求中断后服务端清理，无悬挂段）
- [ ] 双客户端同挂验证：一个退出后服务端仍活（另一个心跳续命）；两个都退后 5 分钟自退
- [ ] `pnpm -r verify` 绿