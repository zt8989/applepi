{
  "name": "@applepi/bundle",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @applepi/core build && pnpm --filter @applepi/extension build && tsc -p tsconfig.json",
    "test": "pnpm --filter @applepi/core build && pnpm --filter @applepi/extension build && tsc -p tsconfig.json && node test/bundle.mjs"
  },
  "dependencies": {
    "@applepi/core": "workspace:*",
    "@applepi/extension": "workspace:*",
    "zod": "^3.23.0"
  }
}
