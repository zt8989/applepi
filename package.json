{
  "name": "harness",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @applepi/web dev",
    "dev:web": "pnpm --filter @applepi/web dev",
    "test": "pnpm -r test",
    "verify": "pnpm -r build && pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
