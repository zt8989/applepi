# WorkBuddy project data (not a temp cache — keep out of git)
.workbuddy/

# pnpm store accidentally created inside the workspace (should live outside the repo)
.pnpm-store/

# ZCode session working data
.zcode/

# git worktrees
.worktrees/

# deps / build
node_modules/
dist/
*.tsbuildinfo

# tsx/esbuild runtime temp files
_tmp_*

# logs
*.log

# harness runtime/test artifacts
_smoke_tmp.txt
_deny_sentinel.txt
_empty_ext_dir/
harness-memory.json
.next/

# vision-toolkit session temp
.dsh-vision-toolkit/
